# {{user_name}}'s Nucleus vault

> Your second brain + business OS. AI writes here. You write here. Same files.

## Today

- [[briefs/{{today}}|Today's brief]]
- [[memory/DASHBOARD|Auto-recall dashboard]]
- [[memory/user|My profile]]

## Memory

- [[memory/index|Memory catalog]] — every node, grouped by type and decay state
- [[memory/.decay-config|Decay thresholds]] — hand-editable

## Active engagements

```dataview
TABLE WITHOUT ID file.link AS Client, file.mday AS "Last modified"
FROM "memory/client"
WHERE !contains(file.folder, "archive")
SORT file.mday DESC
```

_(Install the Dataview community plugin to render this block.)_

## Active people

```dataview
TABLE WITHOUT ID file.link AS Person, file.mday AS "Last modified"
FROM "memory/person"
WHERE !contains(file.folder, "archive")
SORT file.mday DESC
LIMIT 20
```

## Active topics

```dataview
TABLE WITHOUT ID file.link AS Topic, file.mday AS "Last modified"
FROM "memory/topic"
SORT file.mday DESC
LIMIT 20
```

## Capability map (Nucleus router)

The Nucleus router lives in your Claude session, not in this vault. To see every capability, ask Claude `/route` (or just speak naturally — the router will translate).

Common operations:
- "What's on my plate today" → `/brief`
- "I just met X" → `/remember`
- "Wrap up the day" → `/end-day`
- "Plan tomorrow" → `/plan-tomorrow`
- "What's stale or contradicted in my memory" → `/research-gaps`
- "Regenerate the memory catalog" → `/reindex`

## Recommended community plugins (install via Obsidian → Settings → Community plugins)

- **Dataview** — renders the queries above and lets you write your own.
- **Tasks** — pull actionable TODOs from anywhere in the vault.
- **Calendar** — sidebar calendar with hot-key jump to today's brief.
- **Periodic Notes** — extend daily-notes to weekly/monthly notes mirroring `/end-week`.

## Mobile

Install Obsidian on phone (free). Sync this folder via iCloud, Obsidian Sync, or any folder-sync tool. Your second brain travels with you; Claude keeps writing to it from your desk.

---

_Generated by `/setup-obsidian` on {{today}}. Hand-edit anything; the setup command will not overwrite this file on re-run._
